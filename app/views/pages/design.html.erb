<h4>Category icons for reference</h4>
<ul>
  <% Dir.glob("#{Rails.root}/app/assets/images/categories/*") do |icon| %>
      <li><%= image_tag("#{Rails.root}/app/assets/images/categories/#{icon}", :class => "style_image") %>
      </li>
  <% end %>
</ul>
